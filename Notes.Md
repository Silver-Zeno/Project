
Step 1 
install: python -m pip install fastapi "uvicorn[standard]" httpx

Step 2
test it if running
    run this in terminal: python -m uvicorn server:app --host 0.0.0.0 --port 3000

Step 3
    python -m http.server 5500

Step 4 
    Access this to web: http://localhost:5500/index.html

Notes 
    create modelfile first 
        ollama create barangaybot -f Modelfile
    





     

